# ============================================================================
#  基础与核心功能
# ============================================================================
CONFIG_BT_DIS_MANUF_NAME_STR="A1WJUN"
CONFIG_BT_DIS_MODEL_NUMBER_STR="KBD47"
# 注意：CONFIG_ZMK_KEYBOARD_NAME 已统一在 boards/shields/A1WJUN-KBD47/Kconfig.defconfig 中定义为 "A1WJUN-KBD47"
# （避免两处赋值不一致）
# 解锁蓝牙ID字符16 理论最大为248字符
CONFIG_BT_DEVICE_NAME_MAX=16
CONFIG_ZMK_COMBO_MAX_PRESSED_COMBOS=10

# ZMK Studio 支持（解锁后可在线配置，保持锁定以防误操作）
CONFIG_ZMK_STUDIO=y
CONFIG_ZMK_STUDIO_LOCKING=y

# ============================================================================
#  HID 与系统行为（稳定优先）
# ============================================================================
# 使用 Boot Keyboard (6KRO) 模式，兼容 BIOS/UEFI 及老旧系统
CONFIG_ZMK_HID_REPORT_TYPE_NKRO=n
CONFIG_ZMK_HID_KEYBOARD_NKRO_EXTENDED_REPORT=n

# 关闭 HID 指示灯，节省电量
CONFIG_ZMK_HID_INDICATORS=n

# ============================================================================
#  鼠标仿真（性能优先）
# ============================================================================
CONFIG_ZMK_POINTING=y
CONFIG_ZMK_POINTING_SMOOTH_SCROLLING=y

# ============================================================================
#  蓝牙与连接（稳定优先，兼顾低延迟）
# ============================================================================
# 发射功率 +4 dBm
CONFIG_BT_CTLR_TX_PWR_PLUS_4=y

# 实验性功能
CONFIG_ZMK_BLE_EXPERIMENTAL_FEATURES=y

# 1M PHY（省电稳定）
CONFIG_BT_CTLR_PHY_2M=n
CONFIG_BT_CTLR_PHY_CODED=n

# 低延迟连接间隔（优化：12→18，更省电，桌面使用场景延迟仍可接受）
CONFIG_BT_PERIPHERAL_PREF_MIN_INT=18
CONFIG_BT_PERIPHERAL_PREF_MAX_INT=20

# 允许主机延迟响应（优化：2 → 4，更省电）
CONFIG_BT_PERIPHERAL_PREF_LATENCY=4

# 解决 Windows 断连问题
CONFIG_BT_GATT_ENFORCE_SUBSCRIPTION=n

# 电池服务
CONFIG_BT_BAS=y
CONFIG_ZMK_BATTERY_REPORTING=y
# 电池上报间隔：120 秒（默认）
# 推荐值：
#   - 重度使用：60 秒（频繁更新，电量更准确）
#   - 普通使用：120 秒（平衡方案）
#   - 轻度使用：180 秒（省电优先）
#   - 省电模式：300 秒（最大化续航）
CONFIG_ZMK_BATTERY_REPORT_INTERVAL=120

# Device Information Service
CONFIG_BT_DIS=y

# ============================================================================
#  电源管理（续航优化）
# ============================================================================
CONFIG_ZMK_SLEEP=y

# 空闲超时：60 秒
CONFIG_ZMK_IDLE_TIMEOUT=60000

# 睡眠超时：3 分钟
CONFIG_ZMK_IDLE_SLEEP_TIMEOUT=180000

# 软关机
CONFIG_ZMK_PM_SOFT_OFF=y

# DCDC 高压模式（通过 boards/shields/A1WJUN-KBD47/Kconfig.defconfig 默认启用，
# 兼容 ZMK 最新版/Zephyr 4.1：无 prompt 的 SOC_DCDC_NRF52X_HV 不能在 .conf 里直接设置）

# ============================================================================
#  性能与资源
# ============================================================================
CONFIG_ZMK_LOG_LEVEL_DBG=n

# 连接数限制（与 Bluetooth 层 BT_SEL/BT_DISC 0~4 共 5 个保持一致）
CONFIG_BT_MAX_CONN=5
CONFIG_BT_MAX_PAIRED=5

# ============================================================================
#  编码器
# ============================================================================
CONFIG_EC11=y
CONFIG_EC11_TRIGGER_GLOBAL_THREAD=y

# ============================================================================
#  高级调优（优化版）
# ============================================================================
# 去抖优化：5ms（机械轴量产批次更稳定，避免双击，与 overlay debounce-*-ms 保持一致）
CONFIG_ZMK_KSCAN_DEBOUNCE_PRESS_MS=5
CONFIG_ZMK_KSCAN_DEBOUNCE_RELEASE_MS=5
